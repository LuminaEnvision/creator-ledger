# Suppress peer dependency warnings for known compatible versions
# TypeScript 5.9.3 is compatible with @farcaster/quick-auth despite the peer dependency warning
legacy-peer-deps=false
strict-peer-deps=false

